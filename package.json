{
  "name": "textory",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix react-editor run dev",
    "build": "npm --prefix react-editor run build",
    "preview": "npm --prefix react-editor run preview",
    "install:app": "npm --prefix react-editor install"
  }
}
